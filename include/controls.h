#ifndef __CONTROLS_H
#define __CONTROLS_H

struct Controls
{
  Controls();

  // TODO:
  // Check if the compiler generates a reasonable copy constructor
  Controls(const Controls &c) = default;

  // TODO: set values
};

#endif
